# Secrets
.credentials.json

# Caches and temp data
cache/
paste-cache/
stats-cache.json
mcp-needs-auth-cache.json

# Session data and history
sessions/
history.jsonl
channels/
shell-snapshots/
session-env/

# IDE integration
ide/

# Backups and file history
backups/
file-history/

# Telemetry
telemetry/

# Tasks and todos (ephemeral)
/tasks/
/todos/
/plans/
.tasks.toml

# Debug
debug/

# Chrome/browser
chrome/

# Plugins (installed from marketplace, not custom config)
plugins/

# Agent teams
teams/

# Review logs
review-logs/
review/logs/

# Project-local (nested) claude config when running inside ~/.claude
/.claude/

# Claude Code runtime data (session transcripts + per-project memory) — state, not config
projects/

# Transcripts
transcripts/

# Runtime state
.last-cleanup
.last-update-result.json

*.pyc
__pycache__/

# Coverage artefacts (generated by pytest --cov / coverage run; consumed by preflight gate)
coverage.xml
.coverage
.coverage.*
htmlcov/
lcov.info
.pytest_cache/
.ruff_cache/
